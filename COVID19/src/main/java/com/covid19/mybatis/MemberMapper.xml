<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.covid19.mybatis.MemberMapper">

	<select id="getSelectOneMember" resultType="MemberBean" parameterType="Integer">
		SELECT * FROM MEMBER02 WHERE NO = #{no}
	</select>
	
	<select id="getPasswordMember" resultType="String" parameterType="Integer">
		SELECT PASSWORD FROM MEMBER02 WHERE NO = #{no}
	</select>
	
	<delete id="deleteMember" parameterType="Integer">
		DELETE FROM MEMBER02 WHERE NO = #{no}
	</delete>
	
	<update id="updateMember" parameterType="MemberBean">
		UPDATE MEMBER02 SET NAME = #{name},
		                    EMAIL = #{email},
		                    PHONE = #{phone},
		                    INTEREST = #{interest},
		                    AGE = #{age} WHERE NO = #{no}
	</update>
	<insert id="insertMember" parameterType="MemberBean">
		INSERT INTO MEMBER02 VALUES (MEMBER_SEQ.NEXTVAL,
									#{id},
									#{name},
									#{password},
									#{email},
									#{phone},
									#{interest},
									#{age},
									#{profileImg}
									)
	</insert>
	
	<select id="idCheck" parameterType="String" resultType="Integer">
		SELECT COUNT(*) AS COUNT FROM MEMBER02 WHERE ID = #{ID} 
	</select>
	
	<select id="getLoginMember" parameterType="MemberBean" resultType="MemberBean">
		SELECT * FROM MEMBER02 WHERE ID = #{id} AND PASSWORD = #{password}
	</select>
</mapper>










